try:
        #     if line.find(s)!=-1:
        #         payload.append(s)
        #         writer.writerow(payload)
        #         browser.quit()
        #         elem.clear()
        #         browser.back()
        #         continue
        #     elif line.find(g)!=-1:
        #         payload.append(g)
        #         writer.writerow(payload)
        #         elem.clear()
        #         browser.back()
        #         continue
        #     else:
        #         writer.writerow(payload)
        #         elem.clear()
        #         browser.back()
        #         continue
        # except:
        #     browser.quit() 
